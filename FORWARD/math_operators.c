#include <stdio.h>
/*
    + (addition)
    - (subtraction)
    * (moltiplication)
    / (division)
    % (modulus) means  "the rest of a division"
    ++ (increment)
    -- (decrement)

    x = x + 2
    x += 2

    x = x - 3
    x -= 3

    x = x * 4
    x *= 4

    x = x / 5
    x /= 5

    x = %2
    x %= 2
*/


int main() {


    return 0;
}